#include <stdio.h>

int main(){

    char nome ;
    double fixo, total,salario, venda, porc;
    nome = "PEDRO";
    fixo = 700.00;
    total = 0.00;
    porc = 15;
    venda = (porc*total)/100;
    salario = fixo + venda;


    printf("TOTAL = R$ %.2f \n\n", salario);


return 0;
}
