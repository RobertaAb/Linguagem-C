#include <stdio.h>
#include <math.h>

int main(){
    int A, B, C, D, PAB, PCD, DIFERENCA;
    A = 5;
    B = 6;
    C = 7;
    D = 8;
    PAB = A *B;
    PCD = C * D;
    DIFERENCA = PAB - PCD;

        printf("DIFERENCA = %d \n\n", DIFERENCA);


return 0;
}
